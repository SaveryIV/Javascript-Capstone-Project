module.exports = {
  testEnvironment: 'node', // Specifies the environment in which tests are run
//   testEnvironment: 'jest-environment-jsdom',
  testMatch: ['**/__tests__/**/*.js?(x)', '**/?(*.)+(spec|test).js?(x)'], // Specifies test file patterns
};
